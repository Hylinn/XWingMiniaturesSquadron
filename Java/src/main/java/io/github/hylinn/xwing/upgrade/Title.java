package io.github.hylinn.xwing.upgrade;

import io.github.hylinn.xwing.constraint.upgrade.UpgradeConstraint;

public class Title extends Upgrade {
    public Title(UpgradeConstraint constraint) {
        super(constraint);
    }
}
